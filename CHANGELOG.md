# Changelog

Completed backlog items move here from `.claude/napkin.md`'s Backlog category, most recent first. Napkin keeps only pending work; this file is the history.

## Unreleased

- **[2026-08-08] LAN mode M1 wire-up spike: lobby UI + host-authoritative match** — closes out Plan 10's M1 milestone. `Assets/Scripts/UI/LanLobbyScreen.cs` is a procedural Host/Join/Ready lobby (Canvas/Button/InputField/Toggle built entirely in code, no hand-authored UI assets) wired to `LanSessionLauncher`. `Assets/Scripts/Networking/LanMatchBootstrap.cs` spawns two real `NetworkObject` pawns (host-owned + guest-owned) once the host starts the match, and `LanMatchLoopHost` (a Networking-owned analog of `Core/MatchLoopHost`, kept separate so Netcode types never leak into transport-agnostic `Core`) ticks a host-only `MatchController` and mirrors its state into `NetworkMatchState`/`NetworkSailorState` each frame. `NetworkPawnPresenter` disables `SailorPawn`'s local simulation on every non-host peer so only the host ever runs authoritative combat logic — clients render via `NetworkTransform` + the mirrored state. Since NGO can only resolve a spawn across peers using a `GlobalObjectIdHash`, which only exists for on-disk prefab assets, a new headless tool (`Tools > Blue Water Riptide > Build LAN Network Prefabs`, `NetworkPawnPrefabSetup.cs`) bakes the two network prefabs the bootstrap instantiates — the first place this project's pure-runtime-construction pattern needed a real prefab asset, for a structural NGO reason. `LanModeEntryPoint` pushes the lobby as an always-on overlay independent of `M1PrototypeBootstrap`, which is untouched and still the default flow. Compile-verified headlessly, 0 errors. Not yet verified end-to-end with two actual connected instances — no second machine/emulator available in this environment; that's the next LAN task.
- **[2026-08-08] Headless integration wiring: real AI opponent, arena greybox prefab, NetworkManager scene wiring, UI nav-stack** — four independent tasks built in parallel via worktree-isolated agents, merged and compile-verified clean. `M1PrototypeBootstrap` now fights a real utility-state `AIInputDriver` (via a new `LiveSailorPerceptionSource` adapter) instead of the stationary `DummyAIInputDriver`, and calls `ProgressionService.ApplyMatchResult` on `MatchController.OnMatchEnd` (new `MatchController.RoundTimeRemaining` getter feeds the AI's timeout awareness) — a played Single Player match now actually earns trophies/Doubloons. `Assets/Editor/BuildTools/ArenaAuthoring.cs` and `NetworkSceneSetup.cs` (new headless `-executeMethod` tools, same pattern as `RenderPipelineSetup.cs`) were written and then run: produced a real Tideline Cove greybox prefab at `Assets/Resources/Environment/TidelineCove.prefab`, and added a configured `NetworkManager`+`UnityTransport` to `Boot.unity`. `Assets/Scripts/UI/Navigation/` adds `UIStateController`/`UIScreen`, Plan 04's nav-stack plumbing, with a runtime screen-registration API needing no Inspector wiring. Neither the arena prefab nor the LAN NetworkManager are wired into `M1PrototypeBootstrap` yet (still building its old inline greybox / still Single-Player-only) — that's Editor-bound follow-up, tracked in the napkin backlog.

- **[2026-08-08] Implementation plans: graphics (09), LAN mode (10), automation (11)** — comprehensive roadmaps for delivering a playable LAN alpha. Plan 10 targets a minimal 1v1-match spike (procedural visuals, no art needed) as backlog priority #1 to validate gameplay over network early. Plan 09 details Sailor/arena/audio/props sourcing + AI prompts for batch submission. Plan 11 codifies the automation loop (compile verification → build → install → launch → screenshot verification). Also generated 11 AI-service prompts (Sailor portraits/pawns, arena/props, audio) in `.claude/prompts/` for Gemini/Tripo/Suno batch submission.
- **[2026-08-08] Compile verification completed: LAN code verified, one CS0118 error fixed** — Ran `Unity.exe -batchmode -nographics -quit` (headless compile) on the 2026-08-07 AI/LAN/Progression code batch. Found one error: `Networking/Session/LanSessionLauncher.cs`'s unqualified `Session` reference resolved to the enclosing namespace instead of `BlueWaterRiptide.Core.Session` (namespace shadowing). Fixed by fully qualifying to `Core.Session`. Re-ran compile: 0 errors (4 pre-existing unrelated warnings in `M1PrototypeBootstrap.cs` — `FindFirstObjectByType` obsolete). All LAN/AI/Progression code is now compile-verified and ready to wire.
- **[2026-08-07] File-only backlog sweep: AI, LAN networking, progression, online-readiness seam** — closed out every remaining `[File]`-tagged backlog item that didn't need the Unity Editor open. Single Player AI (`Assets/Scripts/AI/`, `Core/Modes/`): real utility-state opponent (`AIInputDriver`, `AIPerception`, `AIBehaviorProfile` Serve/Spike presets, `AIDifficulty` Deckhand/Bosun/Skipper tiers, `AISquadBrain`, `AITeamBuilder`, `SinglePlayerLauncher`) — not yet wired into the live prototype, which still runs `DummyAIInputDriver`. Local Network scaffolding (`Assets/Scripts/Networking/`): UDP LAN discovery (`LanSessionAdvertiser`/`Scanner`), `LobbyState` + its NGO-replicated mirror, rejoin-token bookkeeping, `NetworkInputDriver`/`NetworkSailorState`/`NetworkMatchState`/`NetworkAbilityRelay`, `LanSessionLauncher` implementing the new `ISessionProvider` seam — none of it wired into a scene's `NetworkManager` yet. Progression (`Core/ProgressionService.cs` + save schema v2): trophies, Fleet Rank tiers, Commendations catalog, first-win-of-day bonus, Sailor level-ups — not yet called from a Results flow. Plus the `ISessionProvider` interface (Plan 03's online-readiness seam) and the last two ability-framework primitives (`WallSpawnAbilityBehavior`, `DashAbilityBehavior`). All of it is plain C#/data, reviewed for internal consistency but unverified by a Unity compile (no Editor available this session) and not yet integrated into `M1PrototypeBootstrap`.
- **[2026-07-19] Prototype now playable on Android, with real touch controls** — left-side virtual joystick to move, right side drag-to-aim/hold-to-fire, and a Super button zone above it, matching the intended mobile control scheme. Also fixed the Android build actually rendering anything (it was silently running on the wrong graphics pipeline before) and a device-only bug where the game objects' hitboxes failed to attach. Editor testing still uses keyboard/mouse for faster iteration; touch is what runs on-device.
- **[2026-07-18] First playable prototype: Ensign Ace vs. Admiral Anchor** — a local, Editor-only build where you can move, aim, and fight as either Sailor. Ace fires a ranged 3-shot Jump Serve burst; Anchor swings a close-range Anchor Slam that knocks enemies back, and once its Super charges up, leaps in with Drop Anchor for impact damage plus a temporary damage-reduction shield for nearby allies. Knockouts end the match. Keyboard/mouse only for now — no touch controls or on-device build yet, and the arena is a placeholder greybox, not the real Tideline Cove.
- **[2026-07-18] Bootstrap the Unity project** — Unity 6000.5.4f1 LTS opened and building for Android: IL2CPP/ARM64, URP, Input System (New), custom base Gradle template (kotlin-stdlib duplicate-class fix), Optimized Frame Pacing disabled (Swappy/Vulkan emulator crash workaround). Confirmed running on `Pixel_9_API_35` emulator.
